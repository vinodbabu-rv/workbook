apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ipam-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ipam-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ipam-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
